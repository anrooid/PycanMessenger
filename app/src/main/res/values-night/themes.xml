<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.PycanMessenger" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/dark</item>
        <item name="colorPrimaryVariant">@color/dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/dark_ac</item>
        <item name="colorSecondaryVariant">@color/dark_ac</item>
        <item name="colorOnSecondary">@color/dark_ac</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@color/dark</item>
        <item name="android:navigationBarColor">@color/dark</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textColorHint">@color/white_hint</item>
    </style>
    <style name="Theme.FloatingActionButton" parent="Widget.MaterialComponents.FloatingActionButton">
        <item name="backgroundTint">@color/dark</item>
        <item name="tint">@color/dark_ac</item>
        <item name="rippleColor">@color/black_hint</item>
    </style>

    <style name="Theme.Vector" parent="Widget.AppCompat.ImageButton">
        <item name="tint">@color/dark_ac</item>
    </style>

    <style name="Theme.Tab" parent="Widget.MaterialComponents.TabLayout">
        <item name="android:background">@color/dark</item>
        <item name="tabIndicator">@drawable/tab_indicator</item>
        <item name="tabTextColor">@color/dark_ac</item>
        <item name="tabSelectedTextColor">@color/white</item>
        <item name="tabIndicatorFullWidth">false</item>
        <item name="tabIndicatorGravity">bottom</item>
        <item name="tabIndicatorColor">@color/white</item>
        <item name="tabIndicatorHeight">4dp</item>
        <item name="tabRippleColor">@color/dark</item>
    </style>
    <style name="Theme.Toolbar" parent="Widget.MaterialComponents.Toolbar">
        <item name="android:background">@color/dark</item>
    </style>
</resources>